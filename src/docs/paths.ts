import {
  logupPath,
  loginPath,
  usersPath,
  entriesPath,
  mePath
} from './paths/';

export default {
  '/logup': logupPath,
  '/login': loginPath,
  '/me': mePath,
  '/users': usersPath,
  '/entries': entriesPath
};
